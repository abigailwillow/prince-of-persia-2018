using UnityEngine;

public class FireFlicker : MonoBehaviour
{

/*    public float minIntensity = 0.5f;
    public float maxIntensity = 2f;
    public float flickerSpeed = 0.1f;
    public float smoothAmount = 0.1f;

    Light fireLight;
    float startHeight;

    void Awake()
    {
        fireLight = GetComponent<Light>();
        InvokeRepeating("DoFlicker", flickerSpeed, flickerSpeed);
    }

    void DoFlicker()
    {
        fireLight.intensity = Mathf.Lerp(fireLight.intensity, Random.Range(minIntensity, maxIntensity), smoothAmount);
    }

    void Update()
    {
        print(fireLight.intensity);
    }*/
}
